name: PR tests 2

on:
  pull_request:
    branches: [main]
  issue_comment:
    types: [created]

jobs:
  pr21:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "PR test 2 1"
          sleep 31
  
  pr22:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "PR test 2 2"
          sleep 31
